name: Keep Website Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # Every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
    - name: Ping website
      run: |
        curl -s https://telegramforwarder-5gl5.onrender.com > /dev/null
        echo "✅ Website pinged at $(date)"
